logging:
  config: classpath:logback.xml
  level:
    com:
      itstudio:
        bookservice: 'OFF'
spring:
  main:
    banner-mode: 'off'
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    hikari:
      maximum-pool-size: 2
    password: ''
    url: jdbc:tc:postgresql:17.0:///bookServiceApp?TC_TMPFS=/testtmpfs:rw
    username: bookServiceApp
